version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
  build:
    commands:
      - aws cloudformation package --template-file template.yaml --s3-bucket
        jm11065-codepipeline-artifacts --output-template-file packaged.yaml
artifacts:
  files:
    - packaged.yaml
